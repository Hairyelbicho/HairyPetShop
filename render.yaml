# Render — desactivado a propósito (17 ago 2026).
# n8n-hairypetshop, hairy-utils y los cron ya no se despliegan desde este repo.
# La automatización vive en la API propia (VPS Docker). No vuelvas a activar autoDeploy
# de n8n o un push a GitHub recrearía el servicio de pago.
#
# Si Render sigue mostrando el servicio: Dashboard → n8n-hairypetshop → Settings → Delete service.
# Hostinger: borrar CNAME n8n → n8n-hairypetshop.onrender.com

services: []
cronJobs: []
